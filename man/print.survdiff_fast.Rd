% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.survdiff_fast.R
\name{print.survdiff_fast}
\alias{print.survdiff_fast}
\title{Print Method for survdiff_fast Objects}
\usage{
\method{print}{survdiff_fast}(x, digits = max(1L, getOption("digits") - 3L), ...)
}
\arguments{
\item{x}{An object of class \code{"survdiff_fast"} returned by
\code{\link{survdiff_fast}}.}

\item{digits}{Number of significant digits to display. Defaults to the
global option \code{getOption("digits")}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Invisibly returns \code{x}.
}
\description{
Formats and prints a \code{survdiff_fast} object similarly to
\code{print(survival::survdiff(...))}, showing the observed and expected
event counts for the control and treatment groups, the per-group
contributions \code{(O-E)^2 / E} and \code{(O-E)^2 / V}, the test
statistic, and the corresponding p-value.
}
\examples{
library(survival)
fit <- survdiff_fast(ovarian$futime, ovarian$fustat, ovarian$rx,
                     control = 1, side = 2)
print(fit)

}
\seealso{
\code{\link{survdiff_fast}}
}
