% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.maxcombo_fast.R
\name{print.maxcombo_fast}
\alias{print.maxcombo_fast}
\title{Print Method for maxcombo_fast Objects}
\usage{
\method{print}{maxcombo_fast}(x, digits = max(1L, getOption("digits") - 3L), ...)
}
\arguments{
\item{x}{An object of class \code{"maxcombo_fast"} returned by
\code{\link{maxcombo_fast}}.}

\item{digits}{Number of significant digits to display. Defaults to the
global option \code{getOption("digits")}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Invisibly returns \code{x}.
}
\description{
Formats and prints a \code{maxcombo_fast} object. It shows the total sample
size, a table of the Fleming-Harrington component Z-scores, and the
max-combo statistic with its p-value. For a one-sided test the statistic is
the most negative component, so a negative value favours the treatment
group, matching the sign convention of \code{\link{survdiff_fast}}. For a
two-sided test the statistic is the largest component in absolute value.
}
\examples{
library(survival)
fit <- maxcombo_fast(ovarian$futime, ovarian$fustat, ovarian$rx,
                     control = 1, side = 1)
print(fit)

}
\seealso{
\code{\link{maxcombo_fast}}
}
