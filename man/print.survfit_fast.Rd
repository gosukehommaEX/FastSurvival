% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.survfit_fast.R
\name{print.survfit_fast}
\alias{print.survfit_fast}
\title{Print Method for survfit_fast Objects}
\usage{
\method{print}{survfit_fast}(x, digits = max(1L, getOption("digits") - 3L), ...)
}
\arguments{
\item{x}{An object of class \code{"survfit_fast"} returned by
\code{\link{survfit_fast}}.}

\item{digits}{Number of significant digits to display. Defaults to the
global option \code{getOption("digits")}.}

\item{...}{Additional arguments (currently unused).}
}
\value{
Invisibly returns \code{x}.
}
\description{
Formats and prints a \code{survfit_fast} object similarly to
\code{print(summary(survival::survfit(...), times = t_eval))}, showing the
Kaplan-Meier survival estimate, the Greenwood standard error on the
survival scale, and the confidence interval at the requested evaluation
time.
}
\examples{
set.seed(42)
t_raw <- rexp(100, rate = 1 / 10)
e_raw <- rbinom(100, 1, 0.7)
ord   <- order(t_raw)
fit   <- survfit_fast(t_raw[ord], e_raw[ord], t_eval = 10)
print(fit)

}
\seealso{
\code{\link{survfit_fast}}
}
