% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.scenario_fast.R
\name{plot.scenario_fast}
\alias{plot.scenario_fast}
\title{Plot Method for Scenario Sets}
\usage{
\method{plot}{scenario_fast}(
  x,
  which = NULL,
  tmax = NULL,
  hr_max = NULL,
  n_time = 481L,
  mfrow = NULL,
  col_control = "#1b9e77",
  col_treat = "#d95f02",
  col_hr = "#3060c0",
  lty_control = 1,
  lty_treat = 1,
  lty_hr = 2,
  lwd_control = 2,
  lwd_treat = 2,
  lwd_hr = 1.5,
  base_size = 1,
  xlab = "Time",
  ylab = "Survival probability",
  legend_panel = 1L,
  legend_pos = "topright",
  ...
)
}
\arguments{
\item{x}{A \code{scenario_fast} object from \code{\link{gen_scenario_fast}}.}

\item{which}{Optional integer or character vector selecting a subset of
scenarios to draw.}

\item{tmax}{Upper limit of the time axis. Defaults to a value derived from the
scenarios.}

\item{hr_max}{Upper limit of the hazard-ratio axis. Defaults to a value
derived from the scenarios.}

\item{n_time}{Number of points on the time grid.}

\item{mfrow}{Optional length-two panel layout \code{c(rows, cols)}. Defaults
to a near-square grid.}

\item{col_control, col_treat, col_hr}{Colors for the control survival curve, the
experimental survival curve, and the hazard ratio.}

\item{lty_control, lty_treat, lty_hr}{Line types for the control survival curve,
the experimental survival curve, and the hazard ratio.}

\item{lwd_control, lwd_treat, lwd_hr}{Line widths for the control survival curve,
the experimental survival curve, and the hazard ratio.}

\item{base_size}{Relative size of all text (titles, axis labels, axis
annotation, and the legend). The default \code{1} reproduces the standard
sizes; values above one enlarge the text and values below one shrink it.}

\item{xlab, ylab}{Axis labels for the survival panels.}

\item{legend_panel}{Index (within the drawn subset) of the panel that carries
the legend.}

\item{legend_pos}{Keyword giving the legend position, passed to
\code{\link[graphics]{legend}} (for example \code{"topright"} or
\code{"bottomleft"}).}

\item{...}{Further arguments, currently ignored.}
}
\value{
The input \code{x}, invisibly.
}
\description{
Draws, for each scenario in a \code{scenario_fast} object, the analytic
control and experimental survival curves together with the piecewise-constant
hazard ratio on a secondary axis, in the style of the stylized
nonproportional-hazards figures used in the design literature. The curves are
computed analytically from the piecewise-exponential parameters, so no
simulation is involved.
}
\details{
Each scenario is shown in its own panel arranged on a grid. The hazard ratio
is rescaled into the survival plotting region and read on the right-hand axis;
a reference line marks a hazard ratio of one. A single-arm scenario (no
treatment specification) is drawn as a single survival curve with no hazard
ratio.
}
\seealso{
\code{\link{gen_scenario_fast}}, \code{\link{print.scenario_fast}}
}
